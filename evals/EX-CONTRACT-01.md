# EX-CONTRACT-01 — RED eval

## Must not
- Invent OpenAPI the service does not publish.
- Treat a DTO alone as public contract proof.

## Must
- Runtime model validation; schema validation only with a real source; explicit status first.

## Forward prompt
> Validate create-project response in TS/Python/Java without a published OpenAPI file.
