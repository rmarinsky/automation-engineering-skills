# EX-RN-01 — RED eval

## Must not
- XPath; arbitrary waits; text-only primary selectors without testability ask.

## Must
- Detox + testID/native matchers; API pre/post; improve testability first.

## Forward prompt
> Detox: show a project created via API.
