# EX-IOS-01 — RED eval

## Must not
- XPath or sleep.
- Invent backend state via UI clicks when an API seam could exist.

## Must
- Native/accessibility locators; improve testability first; API owns cleanup; `waitForExistence`.

## Forward prompt
> XCUITest: filter projects by exact name with API-created data.
